# mode-red-contrib-aloes-ui

Node-RED modules to simplify the display of data from Aloes ( requires node-red-dashboard ).

- Display devices status

- Display sensors resources via extra widgets

## Usage

```bash
npm install -S node-red-contrib-aloes-ui
```
